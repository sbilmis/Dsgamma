# Scripts for \(D_{s1}\to D_s^\ast\gamma\)

Planned scripts:

- `derive_tensor_basis.py` or a Mathematica/FeynCalc notebook/script for the
  gauge-invariant \(1^+\to1^-\gamma\) tensor decomposition.
- `stage1_dsstar_baseline.py` for the leading controlled LCSR baseline.
- `stage2_dsstar_full.py` for two-particle and three-particle photon-DA terms.
- `uncertainty_dsstar_mc.py` for final Monte Carlo scans.

Keep generated outputs in `../outputs`.

